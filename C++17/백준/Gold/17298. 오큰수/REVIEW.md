# AI 코드 리뷰

- Model: `models/gemini-2.5-pro`
- Source: `./C++17/백준/Gold/17298. 오큰수/오큰수.cc`

## 한 줄 결론
- (이 문제는) **[모노토닉 스택]** / 복잡도: O(N) / 스택에 값만 넣으면 안되고 인덱스도 함께 관리해야 하는 점이 핵심.

## 더 빠른/더 안전한 풀이(필수)
- 현 접근이 거의 최선입니다. 스택을 이용한 O(N) 풀이는 이 문제의 시간/공간 복잡도 측면에서 가장 효율적인 표준 해법입니다.

## 내가 지금 틀리기 쉬운 포인트 TOP3
- 1) 초기값/시작점/종료조건: 정답 배열 `ans`를 오큰수가 없는 경우의 값인 -1로 반드시 초기화해야 합니다.
- 2) 인덱스/경계/오버플로/방문처리: `while` 루프에서 `myStack.top()` 접근 전, `!myStack.empty()`를 먼저 검사해야 런타임 에러를 피할 수 있습니다.
- 3) 자료구조/성능 함정: 스택에 값만 저장하면 `ans` 배열의 어느 위치를 갱신할지 알 수 없습니다. 현재 코드처럼 `{인덱스, 값}`을 쌍으로 저장하는 것이 좋은 해결책입니다.

## 즉시 수정 액션(최대 4개)
- 코드가 매우 훌륭하여 즉시 수정할 부분은 없습니다.
- (선택) `typedef` 대신 `using Node = std::pair<int, int>;`를 사용하면 더 모던한 C++ 스타일이 됩니다.
- (선택) `now` 변수를 `for` 루프 내부에 선언하면 변수의 유효 범위가 명확해집니다.

```REVIEW_EXAMPLE
# 제목: 구현 스타일 대안 (인덱스 스택)
- 적용 조건: 코드를 더 간결하게 만들고 싶을 때. 메모리 사용량이 미세하게 줄어듭니다.
- 핵심 아이디어: 입력 배열을 먼저 모두 저장합니다. 스택에는 값 대신 인덱스만 저장하고, 필요할 때마다 `A[index]`로 값을 조회합니다.
- 복잡도: O(N)
- 구현 스케치(C++):
  vector<int> A(n);
  for (int i = 0; i < n; i++) cin >> A[i];
  
  stack<int> s;
  vector<int> ans(n, -1);

  for (int i = 0; i < n; i++) {
      while (!s.empty() && A[s.top()] < A[i]) {
          ans[s.top()] = A[i];
          s.pop();
      }
      s.push(i);
  }
```