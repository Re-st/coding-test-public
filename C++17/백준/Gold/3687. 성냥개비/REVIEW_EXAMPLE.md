# 제목: 더 안전하고 명확한 DP 풀이 (최소값)
- 적용 조건: 현재 풀이의 복잡한 규칙을 이해하고 검증하기 어려울 때.
- 핵심 아이디어: `dp[i]`에 성냥 `i`개로 만들 수 있는 최소값을 문자열로 저장. `dp[i]`는 이전에 계산된 `dp[i-cost]`에 새 숫자를 붙여 만들 수 있는 모든 조합 중 가장 작은 값으로 갱신.
- 복잡도: O(N * L) (N: 성냥 수, L: 최대 자릿수)
- 구현 스케치(C++):